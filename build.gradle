// Add Geckolib dependency placeholder. Replace <geckolib-version> with a compatible version for Forge 1.20.1.
plugins {
    id 'net.minecraftforge.gradle' version '5.1.56' apply false
    id 'java'
}

group = 'com.cosplayhathey'
version = '0.1.0'
sourceCompatibility = '17'
targetCompatibility = '17'

repositories {
    mavenCentral()
    // Add mod repositories for Geckolib / Player Animator as needed
}

dependencies {
    // Forge MDK coordinate must be set by the developer locally. Example:
    // implementation fg.deobf("net.minecraftforge:forge:1.20.1-47.1.0")

    // Geckolib (placeholder) - replace <geckolib-version> with real version supporting 1.20.1
    // implementation fg.deobf("software.bernie.geckolib:geckolib-forge-1.20:<geckolib-version>")

    // Player Animator / Emotecraft - add when you have coordinates
    // implementation fg.deobf("com.example:player-animator:<version>")
}
